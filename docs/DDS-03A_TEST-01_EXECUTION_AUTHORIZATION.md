# DDS-03A / TEST-01 – Execution Authorization

## Status

**AUTHORIZED / NOT YET EXECUTED**

## Scope

This document is the separate execution authorization for:

**DDS-03A / TEST-01 – Reusable 3D Asset Construction**

The execution must conform exactly to the already defined contract:

`docs/DDS-03A_TEST-01_REUSABLE_3D_ASSET_CONSTRUCTION_CONTRACT.md`

No scope expansion is authorized by this gate.

## Authorized objective

Create exactly one minimal reusable 3D asset for **DDS-CHR-001 / Schweinchen 1** using the frozen V1 identity authority as the visual source of truth.

The resulting asset must be suitable for later DDS-03A evidence tests involving multiple views, controlled pose or minimal motion, persistence/reopen behavior, and game-scale readability.

## Explicit boundaries

This authorization does **not** authorize:

- production of the remaining five characters,
- a full animation library,
- gameplay code,
- game-engine integration beyond what is strictly needed to inspect the asset,
- final production topology,
- LOD production,
- facial animation systems,
- cinematic rendering,
- film production,
- redesign of DDS-CHR-001,
- replacement of the frozen V1 identity authority,
- execution of DDS-03A TEST-02 or later tests.

## Evidence requirement

A successful execution must produce a persistent reusable 3D asset, not merely a still render or concept image.

TEST-01 must later be classified only as one of:

- `PASS`
- `PASS WITH LIMITATIONS`
- `FAIL`
- `INCONCLUSIVE`

The classification must be based on the criteria already frozen in the TEST-01 contract.

## Gate result

**DDS-03A / TEST-01 EXECUTION = AUTHORIZED**

No 3D asset is created by this authorization step itself.
